{
  "name": "neexp-monorepo",
  "private": true,
  "workspaces": [
    "npm/*",
    "demo"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "build": "pnpm --filter \"./npm/*\" run build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@changesets/changelog-github": "^0.5.0"
  }
}
