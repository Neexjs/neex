{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.3",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "neex dev --all",
    "build": "neex build --all",
    "lint": "neex lint --all",
    "test": "neex test --all",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "graph": "neex --graph",
    "list": "neex --list",
    "info": "neex --info"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.9.2"
  }
}
